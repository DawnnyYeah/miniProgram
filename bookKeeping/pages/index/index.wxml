<view class="bookingItemList" wx:for="{{bookingItems}}">
  <view class="bookingItem">
    {{item.itemName}}
  </view> 
</view>
<view class="addButton">
  <van-button icon="plus" type="primary" bindtap="addBookingItem()" />
</view>
